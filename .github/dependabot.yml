version: 2
updates:
  # Keep GitHub Actions pinned and current so a floating tag can't silently
  # roll onto a broken release (see riscv/learn#89).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
    labels:
      - "dependencies"
